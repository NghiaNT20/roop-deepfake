--extra-index-url https://download.pytorch.org/whl/cu118

# Thư viện cơ bản
numpy>=1.24.0,<1.25            # mở rộng giới hạn để hài hoà với TF 2.13 và ONNX Runtime
opencv-python==4.7.0.72
onnx==1.14.0
insightface==0.7.3
psutil==5.9.5
tk==0.1.0
pillow==9.5.0
torch==2.0.1+cu118

# 3D và protobuf, tiến trình
open3d==0.18.2
protobuf==4.23.2
tqdm==4.65.0

# ONNX Runtime tuỳ nền tảng
onnxruntime==1.15.0                     ; sys_platform == "darwin" and platform_machine != "arm64"
onnxruntime-silicon==1.13.1             ; sys_platform == "darwin" and platform_machine == "arm64"
onnxruntime-gpu==1.15.0                 ; sys_platform != "darwin"

# TensorFlow – nâng lên 2.13 để tương thích với numpy>=1.24
tensorflow==2.13.0                      ; sys_platform != "darwin"
tensorflow==2.13.0rc1                   ; sys_platform == "darwin"
